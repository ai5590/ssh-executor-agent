server.port=${PORT:25001}
ssh.command.timeout=30
ssh.output.limit=10240

# NEW: where to take SSH keys from
ssh.keys.dir=./keys
# NEW: default private key file name (relative to ssh.keys.dir)
ssh.default.key=agent-ssh-key