version: '3.8'

services:
  ssh-executor-agent:
    build: .
    ports:
      - "25005:8080"
    volumes:
      - ./data:/data:ro
    environment:
      - SERVERS_CONFIG_PATH=/data/servers.json
    restart: unless-stopped
